{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "kamune-daemon-evt-session-reconnected",
  "title": "session_reconnected data",
  "description": "Emitted after transparent session reconnection succeeds.",
  "type": "object",
  "properties": {
    "session_id": { "type": "string" }
  },
  "required": ["session_id"]
}
