{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "kamune-daemon-cmd-rename-session",
  "title": "rename_session params",
  "description": "Renames a live session in memory.",
  "type": "object",
  "properties": {
    "session_id": { "type": "string" },
    "name":       { "type": "string" }
  },
  "required": ["session_id", "name"]
}
